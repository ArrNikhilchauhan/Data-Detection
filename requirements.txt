# --- Web Framework ---
fastapi==0.110.0
uvicorn[standard]==0.27.1

# --- Data Processing ---
# Using fixed versions ensures Render doesn't try to compile 
# a brand new version that doesn't have a binary wheel yet.
numpy==1.26.4
pandas==2.2.1
scipy==1.12.0

# --- Validation & Config ---
pydantic==2.6.4
pydantic-settings==2.2.1

# --- Database ---
# psycopg2-binary is correct for Render, as it doesn't 
# require local libpq development headers.
SQLAlchemy==2.0.27
psycopg2-binary==2.9.9

# --- Metrics & Tools ---
prometheus-client==0.20.0
python-multipart==0.0.9